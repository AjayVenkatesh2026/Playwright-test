<!-- Workspace-specific Copilot instructions -->
- Verify project scaffolding: Vite React + TypeScript, Storybook, Playwright
- Run `npm install` to install dependencies
- Use `npm run dev`, `npm run storybook`, `npm run test:e2e`, and `npm run test:component`
